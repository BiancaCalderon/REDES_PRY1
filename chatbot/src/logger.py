# logger.py
class MCPLogger:
    def log_mcp_request(self, server, method, params):
        # Registrar solicitudes a servidores MCP
        pass
        
    def log_mcp_response(self, server, response):
        # Registrar respuestas de servidores MCP
        pass